## 视频转录功能开发总结 (2026-07-21)

### 环境准备 ✅
- faster-whisper 1.2.1 已安装
- yt-dlp 2026.06.09 已就绪
- ffmpeg 8.1.2 已就绪
- 模型下载：首次启动时自动从 HuggingFace 下载（~2.9GB, 约2-3小时）

### 代码改动（已提交到 v2-http-direct 分支）

| 文件 | 状态 | 内容 |
|------|------|------|
| `local-server/transcriber.py` | 新增 | 长驻转录进程：stdin JSON → yt-dlp下载 → ffmpeg转音频 → faster-whisper转录 → 写入收件箱 |
| `local-server/requirements.txt` | 新增 | faster-whisper, yt-dlp |
| `local-server/server.js` | 修改 | +转录队列 + Python子进程生命周期管理 + `/ping` 状态接口 |
| `obsidian-local-sync/src/pipeline/ContentClassifier.ts` | 修改 | +`transcript` 类型，插件对此类型跳过处理直接搬运到Vault |

### 架构决策
- 长驻进程（模型常驻内存）+ stdin/stdout JSON 通信
- 串行队列（RTX 4060 同一时间只能跑一个 Whisper）
- 转录完成后一次写入收件箱（vs 先pending再更新）
- `source_type: "transcript"` 让 Obsidian 插件跳过重复处理

### 启动方式
```bash
cd E:\AI\项目\obsidian同步软件\local-server
node server.js
```

### 待完成
- [ ] git push（网络超时，网络恢复后手动 `git push origin v2-http-direct`）
- [ ] 首次启动等待模型下载（~2-3小时）
- [ ] 微信视频号下载适配（yt-dlp 可能不直接支持）
